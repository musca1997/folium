{
  "name": "@folium/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp manifest.json popup.html dist/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.306",
    "typescript": "^5.0.0"
  }
}
